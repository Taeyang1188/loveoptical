# RSS와 Sitemap 같은 정적 파일은 리다이렉트하지 않음
/rss.xml  /rss.xml  200
/sitemap.xml  /sitemap.xml  200
/feed.xml  /feed.xml  200
/robots.txt  /robots.txt  200

# 다른 모든 경로는 index.html로 (SPA 라우팅)
/*  /index.html  200
